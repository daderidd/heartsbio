---
// Hero Section - Modern, clean hero with background image
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden -mt-20 pt-20">
  <!-- Background Video with Overlay -->
  <div class="absolute inset-0 top-0 z-0">
    <video
      autoplay
      loop
      muted
      playsinline
      preload="metadata"
      poster="/images/hero-field.jpg"
      class="w-full h-full object-cover"
    >
      <source src="/videos/hero-field.mp4" type="video/mp4" />
      <!-- Fallback image for browsers that don't support video -->
      <img
        src="/images/hero-field.jpg"
        alt="Agricultural field"
        class="w-full h-full object-cover"
      />
    </video>
    <!-- Dark overlay for text readability -->
    <div class="absolute inset-0 bg-gradient-to-b from-black/60 via-black/40 to-dark-green/90"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 container-custom text-center text-white py-20 mt-16">
    <div class="max-w-4xl mx-auto">
      <!-- Main Headline with Rotating Text -->
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-display font-bold mb-6 leading-tight">
        <span id="rotating-word" class="inline-block transition-all duration-500 ease-in-out">Sustainable</span><br />
        Biotechnology Solutions
      </h1>

      <!-- Subheadline -->
      <div class="mb-12 max-w-3xl mx-auto">
        <p class="text-xl md:text-2xl text-white/90 leading-relaxed">
          <span class="font-bold">Stronger Crops. Healthier Soils. Cleaner Waters.</span>
          <br />
          We turn biological intelligence into agricultural performance — helping farmers get more from every hectare.
        </p>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a href="#portfolio" class="btn btn-primary text-lg px-10 py-4 bg-white text-dark-green hover:bg-cream transition-all shadow-lg" aria-label="Explore our R&D portfolio">
          Explore Our Work
        </a>
        <a href="#technology" class="btn text-lg px-10 py-4 border-2 border-white text-white hover:bg-white hover:text-dark-green transition-all" aria-label="Learn about our technology">
          Our Technology
        </a>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 animate-bounce">
    <a href="#technology" class="text-white/80 hover:text-white transition-colors" aria-label="Scroll to technology section">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
      </svg>
    </a>
  </div>
</section>

<style>
  /* Rotating text animation */
  #rotating-word {
    position: relative;
    min-width: 280px;
    text-align: center;
    display: inline-block;
    overflow: hidden;
    height: 1.2em;
  }

  @media (min-width: 768px) {
    #rotating-word {
      min-width: 350px;
    }
  }

  @media (min-width: 1024px) {
    #rotating-word {
      min-width: 420px;
    }
  }

  .word-slide-up-out {
    animation: slideUpOut 0.6s ease-in-out forwards;
  }

  .word-slide-up-in {
    animation: slideUpIn 0.6s ease-in-out forwards;
  }

  @keyframes slideUpOut {
    from {
      opacity: 1;
      transform: translateY(0);
    }
    to {
      opacity: 0;
      transform: translateY(-100%);
    }
  }

  @keyframes slideUpIn {
    from {
      opacity: 0;
      transform: translateY(100%);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Respect user's motion preferences */
  @media (prefers-reduced-motion: reduce) {
    #hero video {
      display: none;
    }
  }
</style>

<script>
  // Rotating text functionality
  const words = ['Sustainable', 'Innovative', 'Efficient', 'Clean'];
  let currentIndex = 0;
  const rotatingElement = document.getElementById('rotating-word');

  function rotateWord() {
    if (!rotatingElement) return;

    // Slide up and out
    rotatingElement.classList.add('word-slide-up-out');

    setTimeout(() => {
      // Change word
      currentIndex = (currentIndex + 1) % words.length;
      rotatingElement.textContent = words[currentIndex];

      // Remove old animation and add slide up in
      rotatingElement.classList.remove('word-slide-up-out');
      rotatingElement.classList.add('word-slide-up-in');

      // Clean up animation class after it completes
      setTimeout(() => {
        rotatingElement.classList.remove('word-slide-up-in');
      }, 600);
    }, 600); // Match animation duration
  }

  // Start rotation
  setInterval(rotateWord, 3000); // Change word every 3 seconds
</script>
